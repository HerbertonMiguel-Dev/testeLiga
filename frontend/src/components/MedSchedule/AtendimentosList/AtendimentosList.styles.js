import styled from 'styled-components';
import { StyledTable as BaseStyledTable } from '../AgendamentosList/AgendamentosList.styles'; // Reutiliza o estilo base da tabela

export const StyledTable = styled(BaseStyledTable)`
  /* Quaisquer estilos adicionais específicos para a tabela de atendimentos */
`;